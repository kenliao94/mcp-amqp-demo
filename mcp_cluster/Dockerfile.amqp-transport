FROM node:20-slim

WORKDIR /app

RUN npm install -g @aws/mcp-amqp-transport

RUN apt-get update && apt-get install -y python3 python3-pip && \
    pip3 install uv --break-system-packages

ENV PATH="/root/.local/bin:${PATH}"

CMD ["mcp-server-amqp-adaptor"]
